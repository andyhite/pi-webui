# Changelog

Completed work, one section per release. Entries are **generated from the commit
range** rather than hand-written: `main` is fast-forward only, so the range
between two tags is exact and gap-free, and Conventional Commit types decide the
grouping. The script that generates them is #94; until it lands this file has no
entries to show, and hand-editing it is not the workaround.

This is the only record of work inside the repository (`AGENTS.md` →
"Documentation"). Decision records live in `docs/decisions/`.

## Unreleased

No release yet. The first tag replaces this section with generated entries.
