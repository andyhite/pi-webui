# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
out/
*.tsbuildinfo

# electron-builder installer output (Epic 8.4) — regenerated by
# `bun run --filter=@plotroom/desktop package`, never committed
apps/desktop/dist-installers/

# scratch build artifact for the #308 shell spike (spike-shell-c308/);
# regenerate with `pnpm --filter @plotroom/web build` copied to this path
apps/web/dist-spike-fixtures/

# agent orchestration runtime artifacts
.pi-subagents/

# caches
.turbo/
.vite/
coverage/
playwright-report/
test-results/

# local state (spec §12: durable state lives outside the repo)
*.db
*.db-journal
*.db-wal
*.db-shm
.plotroom/

# env and secrets
.env
.env.*
!.env.example

# editors and OS
.DS_Store
.idea/
*.swp
.vscode/
