/**
 * @plotroom/web — the renderer entrypoint.
 *
 * One web app for both targets: served by @plotroom/server to a browser, and
 * loaded by @plotroom/desktop in Electron. Never fork the UI per target.
 */

export {};
