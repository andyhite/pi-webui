/**
 * @plotroom/desktop — Electron main process.
 *
 * Spawns a local @plotroom/server or attaches to a remembered remote backend
 * (spec §12), then loads the @plotroom/web renderer.
 */

export const APP_ID = "com.andyhite.plotroom";
