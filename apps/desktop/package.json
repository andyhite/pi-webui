{
  "name": "@plotroom/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/stage-sidecars.mjs && cargo tauri dev",
    "build": "node scripts/stage-sidecars.mjs && cargo tauri build",
    "stage:sidecars": "node scripts/stage-sidecars.mjs",
    "lint": "oxlint --type-aware",
    "lint:rust": "node scripts/ensure-test-stub.mjs && cd src-tauri && cargo clippy --all-targets -- -D warnings",
    "lint:arch": "eslint .",
    "test": "node scripts/ensure-test-stub.mjs && cd src-tauri && cargo test",
    "e2e:build": "node scripts/stage-sidecars.mjs && cd src-tauri && cargo build",
    "e2e": "wdio run e2e/wdio.conf.mjs"
  },
  "description": "PlotRoom desktop shell (Tauri v2 — thin Rust main: window, spawn-or-attach, sidecar lifecycle, single-instance; see #316)",
  "author": "PlotRoom",
  "homepage": "https://github.com/plotroom/plotroom",
  "devDependencies": {
    "@plotroom/eslint-config": "workspace:*",
    "@tauri-apps/cli": "^2.11.4",
    "@wdio/cli": "^9",
    "@wdio/globals": "^9",
    "@wdio/local-runner": "^9",
    "@wdio/mocha-framework": "^9",
    "@wdio/spec-reporter": "^9",
    "@wdio/tauri-service": "^1.3.0"
  }
}
