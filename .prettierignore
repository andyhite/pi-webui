node_modules/
.pi-subagents/
dist/
.turbo/
coverage/
pnpm-lock.yaml
docs/product-spec.md

# Claude Design exports are the reviewable input, byte-for-byte (decision 0002
# §6). Formatting one would make it no longer the export. The provenance README
# beside them is ours and is checked.
docs/design/exports/**/*.dc.html
docs/design/exports/**/support.js
docs/design/exports/**/export-notes.md

# Generated by `renderThemeCss` from the token table, which transcribes §18's
# values verbatim. Prettier normalises `rgba(18,20,23,.40)` to
# `rgba(18, 20, 23, 0.4)` — the same colour and no longer the export's number —
# and it would fight the generator's own formatting on every build.
packages/toolkit/src/theme.generated.css

# Rust/Cargo and Tauri build output (spike-shell-c308/s1-tauri-window and
# any future apps/desktop Tauri shell)
**/src-tauri/target/
**/src-tauri/gen/

# bun build --compile output (apps/session-host's compile script)
apps/session-host/out/
