memory:
  backend: local

autolearn:
  enabled: true

# The foreman delivery workflow (skill://tracker, skill://dev-loop) drives
# GitHub through gh; the github tool adds pr_create, run_watch and search.
github:
  enabled: true
