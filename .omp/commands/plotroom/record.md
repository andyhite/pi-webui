---
description: Record an idea as a GitHub issue (label idea, Backlog)
---

Record an idea in the tracker. The idea, as given:

$ARGUMENTS

If that is empty, ask what the idea is before doing anything.

Read `skill://tracker`, then: distill a plain-sentence title in the house
style, keep the body faithful to the note as given (an idea is a recorded
intention, not a spec — do not embellish it into a proposal; add at most a
line of context or a file/spec reference if one is obviously implied), create
the issue with label `idea`, add it to the board at `Backlog`.

Reply with the issue URL and the title you chose — nothing else.
